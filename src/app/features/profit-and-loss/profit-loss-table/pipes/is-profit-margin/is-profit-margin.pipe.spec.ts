import { IsProfitMarginPipe } from './is-profit-margin.pipe';

describe('IsProfitMarginPipe', () => {
  it('create an instance', () => {
    const pipe = new IsProfitMarginPipe();
    expect(pipe).toBeTruthy();
  });
});
