import { IsInfoCellPipe } from './is-info-cell.pipe';

describe('IsInfoCellPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInfoCellPipe();
    expect(pipe).toBeTruthy();
  });
});
