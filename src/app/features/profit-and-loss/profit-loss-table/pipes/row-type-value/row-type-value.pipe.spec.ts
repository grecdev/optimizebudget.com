import { RowTypeValuePipe } from './row-type-value.pipe';

describe('RowTypeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RowTypeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
